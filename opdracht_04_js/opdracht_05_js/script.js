const paintWalls = function ( collor ){
console.log ("The wall is painted " + collor);
}

const pickedCollor ="purple"

paintWalls (pickedCollor);

const paintWallsSide = function (side, collor ){
    console.log ("The " + side + " wall is painted " + collor);
    }
    const wallSide = "west"
       
      paintWallsSide (wallSide, pickedCollor);